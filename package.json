{
  "name": "backping",
  "version": "1.0.2",
  "description": "A local tray app that lets coding agents ask the user for input over chat.",
  "homepage": "https://sitaramshelke.me/backping/",
  "repository": {
    "type": "git",
    "url": "https://github.com/sitaramshelke/backping.git"
  },
  "bugs": {
    "url": "https://github.com/sitaramshelke/backping/issues"
  },
  "type": "module",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/build-icons.mjs",
    "package:mac": "npm run build && electron-builder --mac dir --publish never",
    "package:win": "npm run build && electron-builder --win nsis --x64 --publish never && node scripts/create-windows-alias.mjs",
    "dmg": "npm run package:mac && node scripts/create-dmg.mjs"
  },
  "keywords": [
    "mcp",
    "codex",
    "telegram",
    "human-in-the-loop",
    "electron"
  ],
  "author": "sitaramshelke",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@slack/socket-mode": "^2.0.7",
    "@slack/web-api": "^7.16.0",
    "electron-store": "^11.0.2",
    "express": "^5.2.1",
    "grammy": "^1.43.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "electron": "^42.1.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "build": {
    "appId": "dev.backping.app",
    "productName": "BackPing",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "src/main/preload.cjs",
      "src/renderer/**/*",
      "assets/**/*",
      "slack-app-manifest.example.json",
      "package.json",
      "README.md"
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "icon": "assets/icon/icon.icns",
      "target": [
        "dir",
        "dmg"
      ],
      "identity": null
    },
    "win": {
      "icon": "assets/icon/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "BackPing"
    },
    "dmg": {
      "title": "BackPing"
    }
  }
}
